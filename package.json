{
  "name": "BackboneBrowserifyReact",
  "version": "0.0.1",
  "dependencies": {
    "backbone": "",
    "jquery": "",
    "react": "",
    "react-dom": "^0.14.8",
    "underscore": ""
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0",
    "babelify": "^7.2.0",
    "watch": "^0.17.1"
  },
  "scripts": {
    "build": "browserify app/app.js -o target/bundle.js",
    "build:watch": "watch 'npm run build' app/",
    "watch": "npm run build:watch"
  },
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "es2015",
            "react"
          ]
        }
      ]
    ]
  }
}
